{
  "name": "ai-gym-planner",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
